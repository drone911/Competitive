#include<bits/stdc++.h>
#define fast ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL)

using namespace std;

int main(){
    fast;
    int T;
    cin>>T;
    while(T--){
        short N;
        cin>>N;
        set<int> a={1, 2, 4, 5};
        cout<<a.size();
        
    }
    return 0;
}