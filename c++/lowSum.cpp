#include<bits/stdc++.h>
#define MAX 200001
typedef long long ll;
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(false);
    short T;
    ll a[MAX];
    ll b[MAX];
    priority_queue<ll> c;
    cin>>T;
    while(T--) {
        int Q, K;
        cin>>K>>Q;
        for(int i = 0 ; i<k ; i++) {
            cin>>a[i];
        }
        for(int i = 0 ; i<k ; i++) {
            cin>>b[i];
        }
    }   
    return 0;
}